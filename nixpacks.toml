[phases.build]
cmds = [
  "cd frontend && npm install && npm run build",
  "cd backend && pip install -r requirements.txt"
]

[phases.start]
cmd = "./start.sh"

[variables]
NODE_VERSION = "18"
PYTHON_VERSION = "3.11"